<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.gradlespringboot.mapper.db1.UserDB1Mapper" >
  <select id="findUserByName" resultType="com.example.gradlespringboot.domain.User" parameterType="String">
    select id, name from user where name = #{name}
  </select>

</mapper>
